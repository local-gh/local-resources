apiVersion: v1
kind: Service
metadata:
  name: nginx-core
  annotations:
    # Required for SSL termination.
    service.beta.kubernetes.io/do-loadbalancer-type: "REGIONAL"

    # Port 80 uses HTTP.
    service.beta.kubernetes.io/do-loadbalancer-protocol: "http"
    service.beta.kubernetes.io/do-loadbalancer-http-ports: "80"

    # Port 443 terminates TLS at DigitalOcean.
    service.beta.kubernetes.io/do-loadbalancer-tls-ports: "443"
    service.beta.kubernetes.io/do-loadbalancer-certificate-name: {{ required "nginx.digitalocean.certificateName is required" .Values.nginx.digitalocean.certificateName | quote }}

    service.beta.kubernetes.io/do-loadbalancer-redirect-http-to-https: "true"
    service.beta.kubernetes.io/do-loadbalancer-disable-lets-encrypt-dns-records: "false"

  labels:
    service: nginx-core

spec:
  type: LoadBalancer

  selector:
    service: nginx-core

  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 80

    - name: https
      protocol: TCP
      port: 443

      # DigitalOcean decrypts HTTPS and forwards HTTP.
      targetPort: 80